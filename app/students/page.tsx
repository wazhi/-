"use client";
import { students } from '@/lib/data';import { Radar, RadarChart, PolarGrid, PolarAngleAxis, ResponsiveContainer } from 'recharts';import { useState } from 'react';
export default function Students(){const [id,setId]=useState(students[0].id);const s=students.find(x=>x.id===id)!;const d=[{k:'编程',v:s.programmingLevel},{k:'数学',v:s.mathLevel},{k:'AI基础',v:s.aiFoundation}];
return <div className='space-y-4'><div className='card'><h1 className='text-xl font-bold'>学生画像</h1><select className='border p-2 mt-2' value={id} onChange={e=>setId(e.target.value)}>{students.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></div><div className='grid md:grid-cols-2 gap-4'><div className='card'><p>{s.name} | {s.major} | {s.grade}</p><p>学习风格：{s.learningStyle} 风险：{s.riskLevel}</p><p>活跃度：{s.activityScore} 平均成绩：{s.averageScore}</p></div><div className='card h-72'><ResponsiveContainer><RadarChart data={d}><PolarGrid/><PolarAngleAxis dataKey='k'/><Radar dataKey='v' stroke='#2563eb' fill='#93c5fd' fillOpacity={0.6}/></RadarChart></ResponsiveContainer></div></div></div>}
