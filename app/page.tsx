import Link from 'next/link';import { diagnoseWeakKnowledge } from '@/lib/algorithms';import { knowledgePoints, students } from '@/lib/data';
export default function Page(){const avg=students.reduce((a,s)=>a+s.averageScore,0)/students.length;const high=students.filter(s=>s.riskLevel==='高风险').length;const tasks=students.length*2;return <div className='space-y-4'><div className='card'><h1 className='text-2xl font-bold'>AI4E-Learn</h1><p>面向工程人工智能课程的个性化学习路径规划平台</p></div><div className='grid grid-cols-2 md:grid-cols-5 gap-3'>{[['学生总数',students.length],['知识点总数',knowledgePoints.length],['平均掌握度',avg.toFixed(1)],['高风险学生',high],['今日推荐任务',tasks]].map(([k,v])=><div key={String(k)} className='card'><div className='text-sm text-slate-500'>{k}</div><div className='text-2xl font-semibold'>{v}</div></div>)}</div><div className='card'><h3 className='font-semibold mb-2'>快速入口</h3><div className='flex gap-2 flex-wrap'>{['students','knowledge-graph','diagnosis','recommendation','resources','analytics'].map(i=><Link key={i} href={`/${i}`} className='px-3 py-2 bg-blue-600 text-white rounded'>{i}</Link>)}</div></div></div>}
