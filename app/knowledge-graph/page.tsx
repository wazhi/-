"use client";
import { knowledgePoints } from '@/lib/data';import { useState } from 'react';
export default function KG(){const [c,setC]=useState('全部');const [id,setId]=useState(knowledgePoints[0].id);const cur=knowledgePoints.find(k=>k.id===id)!;const list=knowledgePoints.filter(k=>c==='全部'||k.category===c);
return <div className='space-y-4'><div className='card'><h1 className='text-xl font-bold'>课程知识图谱</h1><div className='flex gap-2 mt-2'>{['全部','基础知识','机器学习','深度学习','工程应用'].map(x=><button key={x} onClick={()=>setC(x)} className='px-2 py-1 bg-slate-100 rounded'>{x}</button>)}</div></div><div className='grid md:grid-cols-2 gap-4'><div className='card space-y-2'>{list.map(k=><button key={k.id} onClick={()=>setId(k.id)} className='block w-full text-left border rounded p-2'>{k.name} (难度{k.difficulty})</button>)}</div><div className='card'><h3 className='font-semibold'>{cur.name}</h3><p>{cur.description}</p><p>前置：{cur.prerequisites.map(p=>knowledgePoints.find(k=>k.id===p)?.name).join('、')||'无'}</p></div></div></div>}
